# 選填：未設定時仍可使用隔離的訪客離線模式。
# 請只放 Supabase 專案 URL 與前端可公開的 publishable/legacy anon key。
# 絕對不要在 Vite 前端變數放入 sb_secret_、service_role key、資料庫密碼或 OAuth secret；App 會拒絕已知秘密格式。
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-browser-key
